- name: test
  hosts: localhost
  tasks:
    #- name: fetch change tickets for pre validation
    #  action: Fetch_change_tickets query1="short_descriptionLIKEDevice Silencing^start_date<javascript:gs.beginningOfCurrentMinute()^start_date>javascript:gs.endOfYesterday()^state=-2/change_request"
    #  register: result
    #- debug: var=result.response
    #- set_fact:
    #    numbers: "{{ result.response }}"
    #- debug: var=numbers
    #- block:
    #    - debug:
    #        msg: "Call prevalidation role"
    #    - include_tasks: bots/Device silencing/Pre_Validation.yml
    #      loop: "{{ numbers }}"
    #      loop_control: 
    #        loop_var: ch_number
        
        
    #  when: "'string indices must be integers' not in result.response"

    - name: fetch change tickets for post validation
      action: Fetch_change_tickets query1="short_descriptionLIKEDevice Silencing^start_dateRELATIVEGT@minute@ago@15^start_date>RELATIVELT@minute@ago@0/change_request"
      register: result
    - debug: var=result.response
    - set_fact:
        numbers: "{{ result.response }}"
    - debug: var=numbers
    - block:
        - debug:
            msg: "Call prevalidation role"
        - include_tasks: bots/Device silencing/Post_Validation.yml
          loop: "{{ numbers }}"
          loop_control: 
            loop_var: ch_number
        
        
      when: "'string indices must be integers' not in numbers"
 














        

      
    
    


