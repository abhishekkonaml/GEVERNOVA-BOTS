---
 - name: GEV Bots
   hosts: localhost
   #vars:
      #incident_no: "{{ fetch_incident_no }}"
     
      
   tasks:
   # Fetching Incident Details



   
       - set_fact:
            bot_name: "IdleInterval"  
    
       
       - debug:
           msg: "calling node down role"
       - name: call node down role
         include_tasks: bots/IdleInterval/idleinterval.yml
       
   

 

   
   
