---
 - name: Analysis Script
   hosts: localhost
   tasks:
    - action: reassignment_ticket incident='GEVINC0058020' group='HQ CTO Network Enterprise Site Support'
      register: reassignment_ticket_register
    - debug: var=reassignment_ticket_register
   #- block:
   #  - name: Send Mail
   #    action: mailing   result1='{{ output1.response }}'
   #  when: "'success' in output1.status"
   #- block:
   #  - debug: msg="No mail sent as the bot analysis failed"
   #  when: "'failed' in output1.status"